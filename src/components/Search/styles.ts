import styled from 'styled-components';

export const Wrapper = styled.span`
  display: flex;
  flex-direction: row;
  position: relative;
  width: 100%;
`;

export const InputIcon = styled.i`
position: absolute;
top: 12px;
right: 12px;
color: var(--primary);
`;